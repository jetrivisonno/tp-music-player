class Track {
    constructor (id, playlistId, title, url, masterId) {
        this.id = id
        this.playlistId = playlistId
        this.title = title
        this.url = url
        this.masterId = masterId
    }
}

module.exports = Track
